import React from "react";
import ReactDOM from "react-dom";
import App from "./src"; // Make sure this path is correct

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
